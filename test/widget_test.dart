// This is a placeholder smoke test for the AURA app.
// The full app requires async initialization (dotenv + SharedPreferences)
// so the old counter test is replaced with a simple sanity check.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
